apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-deployment-system.fullname" . }}
  labels:
    {{- include "my-deployment-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-deployment-system.selectorLabels" . | nindent 4 }}
