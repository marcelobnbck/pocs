apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-deployment-system.fullname" . }}-test-connection"
  labels:
    {{- include "my-deployment-system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-deployment-system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
